# Install a CUDA-enabled PyTorch build for GPU use; CPU also supported.
torch>=2.0
numpy>=1.23
scipy>=1.9
PyYAML>=6.0
Pillow>=9.1
# Optional perceptual metric (auto mode runs without it when unavailable):
torchvision>=0.15
lpips>=0.1.4
